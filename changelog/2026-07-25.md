# 2026-07-25

## Fixed
- 修复 Shell 工具确认边界：工作区内与 `~/.agents`、`~/.tibis` 读取类路径免确认，读取工作区外路径时请求用户确认。
- Shell 读取类路径确认边界改为优先使用真实路径复核，避免工作区或可信目录内的 symlink 指向外部时绕过确认。
- Shell 安全分析补充识别命令参数字符串中嵌入的路径，避免 `python -c`、`node -e` 等解释器读取工作区外文件时绕过确认。
- Shell 安全分析补充嵌套 shell、`eval`、`find`、`xargs`、Git 破坏性操作、PowerShell 嵌套命令、heredoc、`ln -s` 外部链接和 `~user/` 路径识别。
- Shell runner 与 IPC 增加主进程安全复查、确认码比对、真实路径 cwd 校验，并默认禁用 Bash profile 加载。
- Shell 子进程环境移除 `NODE_OPTIONS`、`PYTHONPATH` 等代码注入型继承变量，降低运行时隐式执行风险。
- 修复工具授权弹窗被用户拒绝时误用 `cancelled` 结果导致 ChatRuntime 停流的问题，拒绝授权现在返回可继续的 `failure/USER_CANCELLED` 结果。

## Changed
- `skills/developing-widget/references/elements-and-bindings.md` 的 Styles 章节改为声明 `style` 接受任意标准 CSS 属性，并在盒型示例中补充 `margin`。
- 将 `skills/developing-widget/references/` 下三份参考文档（`widget-format.md`、`elements-and-bindings.md`、`runtime-api.md`）的说明性文字翻译为中文，代码块与字段名保持英文原样，与 `SKILL.md`、`widget-template` 注释及项目 `AGENTS.md` 语言保持一致。
- 发版 workflow 移除测试用例与 PTY 冒烟验证执行步骤，保留类型检查、lint、构建和发布流程。
- `src/layouts/default/components/HeaderTabMenu.vue` 的菜单样式对齐 `src/components/BDropdown/Menu.vue`：容器统一 `gap`、`line-height`、`border-radius` 与圆角；菜单项改用 `line-height` 控制行高并以 `font: inherit` 继承字体；hover 背景与禁用、危险态、分割线统一改用 `--dropdown-*` 主题变量。
