# 2026-05-02

## Added
- 新增聊天文件引用结构化资源重构设计文档，明确 `file-reference part`、`read_reference` 工具与 snapshot 读取语义。
- 新增聊天文件引用结构化资源实施计划文档，拆分消息结构、snapshot、工具和模型上下文改造任务。
- 新增聊天语音输入设计文档，明确独立 `VoiceInput` 组件、录音波形、边录边转写、多段拼接和 `whisper.cpp` 主进程转写方案。
- 新增聊天语音输入基础实现，包含 `speech` IPC 边界、语音占位块 API、语音转写会话 hook、基础录音/波形 hook 与 `VoiceInput` 组件骨架。
- 新增语音输入侧边栏接线回归测试与 `speech` 环境变量配置支持，`whisper.cpp` 可执行文件、模型文件和临时目录可通过环境变量注入。

## Changed
- 补充文件引用后续实现方向，明确模型上下文从正文预展开切换为结构化引用索引。
- 聊天输入工具栏与侧边栏已接入语音占位块开始/完成流程，语音完成后可替换当前输入占位内容。
- 语音录音链路已支持自动分段、手动切段、队列化等待完成与新会话重置，波形改为居中向两侧展开的布局。
