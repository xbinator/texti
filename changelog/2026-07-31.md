# 2026-07-31

## Added

- 模型平台列表页点击卡片进入详情页时，使用 View Transition API 共享元素缩放转场：详情容器从被点击卡片的视口位置缩放展开填满内容区，列表页保持可见后淡出。新增 `src/views/settings/provider/hooks/useProviderTransition.ts` 封装触发逻辑。
- 聊天输入栏支持为当前会话选择并持久化独立的本地工作区；未选择时继续使用默认 `~/.tibis`。

## Changed

- 关闭仍在运行或等待交互的顶部聊天标签时不再确认或中止 Runtime，而是保留可由 ChatSider 重新接管的后台记录；Runtime 结束后自动清理已分离记录。
- ChatSider 在流式响应或会话列表加载期间继续允许新建、切换、标题编辑、历史列表和打开页面操作，同时继续禁止删除运行中、等待中或正在晋升标签身份的会话。
- Renderer 工具与 Bridge 改为绑定不可变的会话、Runtime、工作区、文档及 WebView 身份，Runtime confirmation 改由应用级 broker 跨 BChat 生命周期交接，并在重连时使用 revision-aware 历史合并避免旧快照覆盖新流事件或混入其他会话消息。
- 会话工作区在读取未完成时阻止 Runtime 启动，读取失败不再静默回退到默认目录；主进程会规范化并校验创建、更新时写入的工作区目录，并隔离目录与文件选择对话框的并发锁。
- 聊天工作区入口仅在独立聊天页的新会话草稿态显示，改为高度 28px、默认透明的 18px 圆角连续胶囊；左侧默认显示文件夹图标，悬停时原位切换为 `x` 恢复操作，名称不移动并使用 12px 字号，且不再使用 BButton。
- 聊天工作区选择入口移至输入工具栏左侧；选择临时目录后可悬停恢复默认 `~/.tibis`，并清除会话保存的覆盖目录。
- 使用 Prettier 格式化 `src/` 与 `electron/` 中未符合配置的代码与样式文件。
- `ProviderCard` 点击改为通过 `useProviderTransition` composable 触发带类型标记的视图过渡，并为根元素添加 `data-provider-id` 以备后续反向 morph。
- `detail.vue` 的 `provider` 与 `models` 初始值改为同步从 store 读取，使视图过渡新快照能立即捕获真实内容，避免 morph 期间显示 spinner。
- `.stylelintrc.cjs` 放行 `active-view-transition` / `active-view-transition-type` 伪类与 `view-transition-*` 伪元素。
- `src/views/settings/service-model/` 目录重命名为 `src/views/settings/serviceModel/`，同步更新路由与测试文件引用，测试目录 `test/views/settings/service-model/` 同步重命名。
- `SidebarItem` 激活时自动调用 `scrollIntoView({ block: 'nearest' })` 滚入可视区域，避免激活项位于滚动容器视口外。
