# 2026-07-26

## Fixed
- 修复富文本编辑器（Rich 模式）从 BChat 点击「插入片段」/文件引用 chip 跳回时无法滚动到指定行范围的问题

## Changed
- `src/components/BEditor/panes/PaneRichEditor.vue` 的 `selectLineRange` 改为在同一事务内通过 `chain().setTextSelection(...).scrollIntoView().run()` 显式触发滚动，不再依赖 `focus()` 命令的延迟滚动行为，与 `PaneSourceEditor.vue` 的 source 模式保持一致

## Added
- 新增 `test/components/BEditor/rich-select-line-range.test.ts` 测试用例，覆盖 Rich 模式 `selectLineRange` 滚动修复的回归保护：
  - 源码级回归：验证 `selectLineRange` 函数体包含 `chain().scrollIntoView()` 调用、显式滚动出现在 `focus()` 之前、保留 `setAISelectionHighlight` 调用
  - Tiptap 行为：验证 `chain().setTextSelection().scrollIntoView().run()` 在编辑器已聚焦时仍会触发 `scrollToSelection`，而 `focus()` 单独调用在已聚焦时会跳过滚动（复现修复前的 bug）

