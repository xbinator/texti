# 2026-07-29

## Changed

- 将 `BubblePartAgentTask.vue` 重构为 `BubblePartAgent/` 目录组件，拆分为 `index.vue`、`AgentTaskHeader.vue` 与 `AgentTaskDetail.vue`，并同步更新 `MessageBubble.vue` 的引用。

## Fixed

- 修正 Electron 主进程编译产物中 `asyncTo` 日志依赖的 ESM 扩展名，避免应用启动时出现模块找不到异常。
- 修复应用冷启动时固定回到欢迎页的问题，启动根路由会恢复上次激活页面。
- 在标题栏标签区新增仪表盘按钮，支持一键回到欢迎页。
- 修复聊天侧栏在拖拽和程序化显隐时错误播放动画、分隔器拖拽手柄被裁剪的问题，仅在顶部切换按钮和侧栏内部关闭按钮操作时启用显隐动画。
- 修复 WebView 页面中 `target="_blank"` 与 `window.open()` 链接点击无响应的问题，安全转发为 Tibis 内的新 WebView 标签页。
