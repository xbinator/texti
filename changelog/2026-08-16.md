# 2026-08-16

## Features
- `src/layouts/default/components/HeaderTab.vue`：关闭标签新增离场动画——锁定宽度后收缩到 0 并淡出，右侧标签从右往左平滑补位；动画结束后才发出 `close` 事件；关闭被守卫拦截（未保存确认取消/并发保护）时直接恢复标签原状；`prefers-reduced-motion` 下跳过动画直接关闭
- `src/layouts/default/components/HeaderTabs.vue`：标签项 wrapper 的右侧间距随离场动画同步收起（`:has(.header-tab.is-closing)`），并补齐 `.header-tab` 的 `width` / `padding` / `border-width` 过渡属性
- `test/layouts/default/header-tab-close-animation.test.ts`：新增关闭动画测试，覆盖宽度锁定收缩、`transitionend` 与兜底超时两种关闭触发、动画期间重复点击忽略、守卫拦截恢复、`prefers-reduced-motion` 直关及样式声明断言

## Changed
- `src/layouts/default/index.vue`：`.b-layout-welcome-tab` 的 `:hover` / `.is-active` / `:active` 状态样式改为 `&` 嵌套写法（规范允许伪类/修饰符嵌套，仅禁止 `&__xxx` / `&--xxx` 生成 BEM 子类名）
- `src/layouts/default/index.vue`：欢迎页标签按钮图标由 `lucide:layout-grid` 换为 `lucide:sparkles`
