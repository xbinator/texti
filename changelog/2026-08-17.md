# 2026-08-17

## Fixed
- `test/components/app-rem-scaling-styles.test.ts`：BSelect tips 区域 padding 断言同步为 `8px 12px 4px`（源码已在 044931de 有意调整，仍保持 px 单位书写）
- `test/components/theme-design-token-styles.test.ts`：同上，同步 BSelect tips padding 期望值
- `test/views/theme-page-token-styles.test.ts`：welcome-tab 的 `:hover` / `.is-active` / `:active` 规则提取改为匹配源码的 `&` 嵌套写法（2026-08-16 样式重构后扁平选择器不再存在）
- `test/layouts/default/header-tabs-chat-status.test.ts`、`test/layouts/default/header-tabs-menu.test.ts`：补齐 jsdom 缺失的 `window.matchMedia` stub（`matches: true` 走减少动效路径使 `close` 事件立即发出），修复关闭标签用例因 `prefersReducedMotion` 抛 `TypeError` 导致的失败与 6 个未捕获异常
