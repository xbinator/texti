# 2026-08-02

## Changed

- BButton 和 Settings 根侧栏导航改用按钮专属边框 Token，默认主题保持无边框，Overworld 通过 `button.borderWidth` 与 `button.border` 显示 2px 像素边框。
- HeaderTab、默认布局欢迎入口和 BDropdownButton 普通态改用按钮专属边框/阴影 Token；显式 `outline` / `bordered` 语义仍保留可见边框。
- Ant Design 主题尺寸派生支持将 `rem` / `em` 自定义尺寸按 16px 基准归一为 number，避免 CSS 变量和 AntD 控件表现分裂。
- 全局主题字体和 Ant Design 输入框主题 reset 合并进 `src/assets/styles/reset.less`，移除独立的 theme font/input 样式入口。
- SettingsTool 的 `themePreset` 取值描述补充 `overworld` 预设（纸面/钴蓝天空/墨线像素边/苔藓绿）。

## Fixed

- 修复全局 Ant Design 输入框阴影覆盖误作用到 `borderless` AInput，导致命令面板搜索框出现外层输入壳与内层输入框双重焦点边框的问题。
- 修复带 prefix/allow-clear 的 AInput 内层 `.ant-input` 仍被全局 focus 样式命中，导致 Provider 侧栏搜索框出现双层边框的问题。
- 修复 Provider 侧栏搜索去掉本地输入壳后缺少 flex 收缩约束，且测试仍按旧的本地圆角样式断言的问题。

## Test

- 修正 `test/components/panel-border-style.test.ts` 边框规则断言为正则，同时兼容 `1px` 和 `var(--surface-border-width)` 两种写法，以匹配尚未统一为变量的 `BEditor/components/Sidebar.vue`。
- 修正 `test/components/theme-input-token-styles.test.ts` 中 `reset.less` 的 `body { ... }` 嵌套结构断言为正则，匹配 Less 嵌套写法而非单行文本。
- 修正 `test/components/BChat/input-toolbar-workspace.test.ts` 工作区圆角断言为 `border-radius: var(--control-radius);`，匹配源码使用的 CSS 变量。
- 修正 `test/components/BCommandPanel/index.test.ts` 中 `icon-stub` 期望为存在，匹配命令面板搜索框实际渲染的 `BIcon` 搜索图标。
- 修正 `test/layouts/default/chat-sider.test.ts` 中 `margin-left: 6px;` 期望归属到 `.chat-sider--visible`，匹配 `ChatSider.vue` 实际样式位置。
